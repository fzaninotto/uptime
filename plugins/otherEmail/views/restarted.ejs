[Restarted] Check "<%= check.name %>" was restarted
On <%= moment(checkEvent.timestamp).format('LLLL') %>,
check "<%= check.url %>" was manually restarted.

<% include details %>
<% include footer %>